# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production
dist/
dist-ssr/
build/

# macOS metadata files
.DS_Store
._*
.AppleDouble
.LSOverride

# Mobile (Capacitor)
# We track google-services.json and GoogleService-Info.plist for collaboration
# but ignore the actual platform build artifacts
# !google-services.json
# !GoogleService-Info.plist

# Editor/IDE
.vscode/
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment / Secrets
# .env files are usually ignored but we've moved public keys to code
.env
.env.local
.env.*.local
.env.production
.env.development

# Firebase (We TRACK these now for your friend)
# firestore.rules
# storage.rules
# firestore.indexes.json

# Secrets
google-services.json